// filters.js — Search, filter, sort, pagination (HOFs only)
//
// Planned functions (all using Array Higher-Order Functions):
// - searchImages(items, query) — Array.filter() on prompt text
// - filterByStyle(items, style) — Array.filter() on style field
// - filterFavorites(items) — Array.filter() on liked flag
// - sortImages(items, criteria) — Array.sort() by date or alphabetical
// - paginate(items, page, perPage) — Array.slice() for pagination
