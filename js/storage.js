// storage.js — localStorage helpers for gallery and preferences
//
// Planned functions:
// - getGallery() — read gallery array from localStorage
// - saveGallery(items) — write gallery array to localStorage
// - addImageToGallery(item) — add a single image object
// - toggleLike(id) — toggle liked status
// - deleteImage(id) — remove image by id
// - getSavedTheme() / saveTheme(theme) — theme preference
